/**
 * Language-independent protocol specification helpers and deterministic signing displays.
 * These JVM helpers support conformance tests; they are not on-chain validators.
 * Recovery helpers model future transitions that Phase 1 state custody rejects.
 */
package com.bloxbean.cardano.kavach.protocol;
